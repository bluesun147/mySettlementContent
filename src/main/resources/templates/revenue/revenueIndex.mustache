{{> header}}

<h1>수익계산서 엑셀 파일 업로드</h1>

<form action="/revenue/upload" method="post" enctype="multipart/form-data">
    <input type="file" name="file" accept=".xlsx">
    <button type="submit">등록</button>
</form>

<hr>

<h1>수익계산서 목록</h1>

<table border="1">
    <thead style="background-color: lightcoral">
    <tr>
        <th>ID</th>
        <th>계약서</th>
        <th>날짜</th>
        <th>수익</th>
        <th>생성일</th>
    </tr>
    </thead>
    <tbody>
    {{#revenueList}}
        <tr>
            <td>{{id}}</td>
            <td>{{contract.id}}</td>
            <td>{{date}}</td>
            <td>{{fee}}</td>
            <td>{{createdAt}}</td>
        </tr>
    {{/revenueList}}
    </tbody>
</table>

<style>
    .pagination { display: flex; list-style: none; padding: 0; justify-content: center!important; flex-wrap: wrap; }
    .pagination li.page-item a { display: block; padding: 0.5em 0.75em; border: 1px solid #dee2e6; margin-left: -1px; text-decoration: none; color: #0d6efd; }
    .pagination li.page-item.active a  { color: #fff; background-color: #0d6efd; }
</style>

<ul class="pagination">
    <li class="page-item disabled"><a class="page-link" href="#">&laquo;</a></li>
    {{#pageIndex}}
        <li class="page-item"><a class="page-link" href="/revenue?page={{.}}">{{.}}</a></li>
    {{/pageIndex}}
    <li class="page-item"><a class="page-link" href="#">&raquo;</a></li>
</ul>